// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
#include <cmath>

void powfunction() {
    long number = pow(2, 50);
    long sol = number - 1;
    cout << sol;
}

int main() {
    powfunction();
    return 0;
}